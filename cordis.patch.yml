# Development preview. Browser tools require a running local Broker, native host
# registration, extension tab consent and DSH approval. See docs/development.md.
- insert:
    - id: native-browser
      name: dsh-native-browser
