# Pre-alpha bundle layer. This mounts only the lifecycle-safe package scaffold;
# no model-facing browser tools are registered until the Chrome vertical slice
# satisfies the security and reliability gates documented in docs/roadmap.md.
- insert:
    - id: native-browser
      name: dsh-native-browser
